<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Edit Event - <%= event.title %></title>
</head>
<body>
    <h1>Edit Event: <%= event.title %></h1>

    <form action="/events/<%= event.id %>/update" method="POST">
        <label>Title:</label>
        <input type="text" name="title" value="<%= event.title %>"><br>

        <label>Date:</label>
        <input type="date" name="date" value="<%= event.date %>"><br>

        <label>Tickets (JSON format):</label>
        <textarea name="tickets"><%= event.tickets %></textarea><br>

        <button type="submit">Save</button>
    </form>

    <a href="/organiser-home">Back to Organiser Home</a>
</body>
</html>