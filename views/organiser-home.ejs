<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Organiser Home Page</title>
</head>
<body style="font-family: 'Segoe UI', Arial, sans-serif; background-color: #f8f9fa; color: #333; margin: 0; padding: 40px;">

    <header style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 30px; border-bottom: 2px solid #dee2e6; padding-bottom: 20px;">
        <h1 style="margin: 0; color: #212529;">Organiser Home Page</h1>
        <a href="/site-settings" style="text-decoration: none; color: #007bff; font-weight: 600; border: 1px solid #007bff; padding: 8px 16px; border-radius: 6px; transition: 0.3s;">
            ⚙️ Site Settings
        </a>
    </header>

    <section id="event-manager-info" style="background: white; padding: 20px; border-radius: 10px; box-shadow: 0 2px 8px rgba(0,0,0,0.05); margin-bottom: 20px;">
        <h2 style="margin-top: 0; color: #007bff;"><%= eventManager.name %></h2>
        <p style="color: #6c757d; line-height: 1.6;"><%= eventManager.description %></p>
    </section>

    <section id="create-event" style="margin-bottom: 30px;">
        <form action="/events/create" method="POST">
            <button type="submit" style="background-color: #28a745; color: white; border: none; padding: 12px 24px; border-radius: 6px; font-size: 1rem; font-weight: bold; cursor: pointer; box-shadow: 0 4px 6px rgba(40, 167, 69, 0.2);">
                + Create New Event
            </button>
        </form>
    </section>

    <section id="published-events" style="background: white; padding: 25px; border-radius: 10px; box-shadow: 0 2px 8px rgba(0,0,0,0.05); margin-bottom: 30px;">
        <h2 style="margin-top: 0; border-left: 5px solid #28a745; padding-left: 15px;">Published Events</h2>
        <table style="width: 100%; border-collapse: collapse; margin-top: 20px;">
            <thead>
                <tr style="background-color: #f1f3f5; text-align: left;">
                    <th style="padding: 12px; border-bottom: 2px solid #dee2e6;">Title</th>
                    <th style="padding: 12px; border-bottom: 2px solid #dee2e6;">Date</th>
                    <th style="padding: 12px; border-bottom: 2px solid #dee2e6;">Created</th>
                    <th style="padding: 12px; border-bottom: 2px solid #dee2e6;">Published</th>
                    <th style="padding: 12px; border-bottom: 2px solid #dee2e6;">Tickets</th>
                    <th style="padding: 12px; border-bottom: 2px solid #dee2e6;">Share</th>
                    <th style="padding: 12px; border-bottom: 2px solid #dee2e6;">Actions</th>
                </tr>
            </thead>
            <tbody>
                <% publishedEvents.forEach(event => { %>
                <tr style="border-bottom: 1px solid #eee;">
                    <td style="padding: 12px; font-weight: bold;"><%= event.title %></td>
                    <td style="padding: 12px;"><%= event.date %></td>
                    <td style="padding: 12px; font-size: 0.85rem; color: #6c757d;"><%= event.createdAt %></td>
                    <td style="padding: 12px; font-size: 0.85rem; color: #6c757d;"><%= event.publishedAt %></td>
                    <td style="padding: 12px;">
                        <% for (let type in event.tickets) { %>
                            <div style="font-size: 0.8rem; background: #e9ecef; display: inline-block; padding: 2px 6px; border-radius: 4px; margin: 2px;"><%= type %>: <%= event.tickets[type] %></div>
                        <% } %>
                    </td>
                    <td style="padding: 12px;">
                        <a href="/attendee/events/<%= event.id %>" style="color: #007bff; text-decoration: none; font-size: 0.9rem;">🔗 Link</a>
                    </td>
                    <td style="padding: 12px;">
                        <form action="/events/<%= event.id %>/delete" method="POST" onsubmit="return confirm('Are you sure?');">
                            <button type="submit" style="background: #fff; color: #dc3545; border: 1px solid #dc3545; padding: 4px 8px; border-radius: 4px; cursor: pointer;">Delete</button>
                        </form>
                    </td>
                </tr>
                <% }); %>
            </tbody>
        </table>
    </section>

    <section id="draft-events" style="background: white; padding: 25px; border-radius: 10px; box-shadow: 0 2px 8px rgba(0,0,0,0.05);">
        <h2 style="margin-top: 0; border-left: 5px solid #ffc107; padding-left: 15px;">Draft Events</h2>
        <table style="width: 100%; border-collapse: collapse; margin-top: 20px;">
            <thead>
                <tr style="background-color: #f1f3f5; text-align: left;">
                    <th style="padding: 12px; border-bottom: 2px solid #dee2e6;">Title</th>
                    <th style="padding: 12px; border-bottom: 2px solid #dee2e6;">Date</th>
                    <th style="padding: 12px; border-bottom: 2px solid #dee2e6;">Created</th>
                    <th style="padding: 12px; border-bottom: 2px solid #dee2e6;">Tickets</th>
                    <th style="padding: 12px; border-bottom: 2px solid #dee2e6;">Edit</th>
                    <th style="padding: 12px; border-bottom: 2px solid #dee2e6;">Actions</th>
                </tr>
            </thead>
            <tbody>
                <% draftEvents.forEach(event => { %>
                <tr style="border-bottom: 1px solid #eee;">
                    <td style="padding: 12px;"><%= event.title %></td>
                    <td style="padding: 12px;"><%= event.date %></td>
                    <td style="padding: 12px; font-size: 0.85rem;"><%= event.createdAt %></td>
                    <td style="padding: 12px;">
                        <% for (let type in event.tickets) { %>
                            <div style="font-size: 0.8rem; background: #e9ecef; display: inline-block; padding: 2px 6px; border-radius: 4px; margin: 2px;"><%= type %>: <%= event.tickets[type] %></div>
                        <% } %>
                    </td>
                    <td style="padding: 12px;">
                        <a href="/events/<%= event.id %>/edit" style="color: #007bff; text-decoration: none;">Edit</a>
                    </td>
                    <td style="padding: 12px;">
                        <form action="/events/<%= event.id %>/publish" method="POST" style="display:inline;">
                            <button type="submit" style="background: #007bff; color: white; border: none; padding: 6px 12px; border-radius: 4px; cursor: pointer; margin-right: 5px;">Publish</button>
                        </form>
                        <form action="/events/<%= event.id %>/delete" method="POST" style="display:inline;" onsubmit="return confirm('Are you sure?');">
                            <button type="submit" style="background: transparent; color: #dc3545; border: none; cursor: pointer; text-decoration: underline;">Delete</button>
                        </form>
                    </td>
                </tr>
                <% }); %>
            </tbody>
        </table>
    </section>
</body>
</html>